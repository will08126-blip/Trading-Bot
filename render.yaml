services:
  - type: worker
    name: trading-bot-discord
    runtime: python
    pythonVersion: "3.11.9"
    buildCommand: pip install -r requirements-discord.txt
    startCommand: python -m app.main --mode discord-bot
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: DISCORD_BOT_TOKEN
        sync: false
      - key: DISCORD_GUILD_ID
        sync: false
      - key: BOT_MODE
        value: paper
      - key: DATABASE_URL
        value: sqlite:///./data/trading_bot.db
      - key: LOG_LEVEL
        value: INFO
      - key: LOG_DIR
        value: ./logs
